import { createTheme } from 'treat';

export const light = createTheme({
  primary: 'red',
  bg: '#ededed',
  text: '#000',
});

export const dark = createTheme({
  primary: 'blue',
  bg: '#909090',
  text: '#fff',
});
